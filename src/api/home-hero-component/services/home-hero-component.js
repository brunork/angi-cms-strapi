'use strict';

/**
 * home-hero-component service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::home-hero-component.home-hero-component');
