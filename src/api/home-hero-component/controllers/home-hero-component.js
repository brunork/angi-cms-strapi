'use strict';

/**
 * home-hero-component controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::home-hero-component.home-hero-component');
